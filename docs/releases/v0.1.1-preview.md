Camera startup and Flip camera fix for Samsung Galaxy S22 and Galaxy Tab A9+.

The previous preview closed MediaPipe's MPImage before drawing or mirroring the same underlying bitmap. MediaPipe recycles that bitmap when the image closes. This could crash the rear preview or display "cannot use a recycled source in createBitmap" when using the front camera.

Version 0.1.1 makes an independent preview copy before releasing the analysis image, including when rotation is zero. The camera still analyzes and displays the same frame; front-camera landmarks remain mirrored with the preview. Temporary analysis images are released on success and failure.

Download **ADhils-Fitness-0.1.1-preview-arm64.apk** and install it over the existing app. The package ID is unchanged and versionCode increases from 1 to 2. Do not uninstall the existing app or clear its storage: your profile and workout data stay in place during a normal update.

Regression coverage uses real Android Bitmap/Canvas and the shipped MediaPipe image wrapper, without needing the ARM64 inference library. It reproduces the original released-image failure and checks rear-preview rotations, front mirroring, and failed-inference cleanup. Real exercise-tracking accuracy and Samsung device performance remain separate validation steps.

Publisher reference: [MediaPipe 0.10.26 BitmapImageContainer.close](https://github.com/google-ai-edge/mediapipe/blob/v0.10.26/mediapipe/java/com/google/mediapipe/framework/image/BitmapImageContainer.java#L40-L43).

Validation completed for this release: all four CameraFrameTest instrumentation tests passed on a fresh Android 15 / API 35 emulator using real Bitmap/Canvas and MediaPipe image wrappers. All 16 core tests passed. APK assembly and lint completed with zero errors and 18 warnings. The APK's signer matches 0.1.0, and the final APK-folder Defender scan completed with no new detection event in the checked interval.

APK SHA-256: ad1d713c046485e66694a0a44189733db2f7a3c7aa157fb97b7b9e5cc9b5b3d0.
